<local:AllViewBase x:Class="WarehouseOperative.Views.AllView.AllOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WarehouseOperative.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="/Views/BookmarksResources.xaml"/>
    </UserControl.Resources>
    <Grid Background="#404040">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
        </Grid>
        <DataGrid Margin="10" Grid.Row="0" Name="DG3" ItemsSource="{Binding List}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Width="*" Header="Order Id" Binding="{Binding Path=OrderId}"/>
                <DataGridTextColumn Width="*" Header="Customer Name" Binding="{Binding Path=CustomersCustomerName}"/>
                <DataGridTextColumn Width="*" Header="Product Name" Binding="{Binding Path=ProductsProductName}"/>
                <DataGridTextColumn Width="*" Header="Order Quantity" Binding="{Binding Path=OrderQuantity}"/>
                <DataGridTextColumn Width="*" Header="Unit Price" Binding="{Binding Path=ProductsUnitPrice}"/>
                <DataGridTextColumn Width="*" Header="Price Paid" Binding="{Binding Path=CustomersPricePaid}"/>
                <DataGridTextColumn Width="*" Header="Order Date" Binding="{Binding Path=OrderDate}"/>
                <DataGridTextColumn Width="*" Header="Required Date" Binding="{Binding Path=RequiredDate}"/>
                <DataGridTextColumn Width="*" Header="Shipped Date" Binding="{Binding Path=ShippedDate}"/>
                <DataGridTextColumn Width="*" Header="Shipper Company Name" Binding="{Binding Path=ShippersShipperCompanyName}"/>
                <DataGridTextColumn Width="*" Header="Supplier Company Name" Binding="{Binding Path=SuppliersCompanyName}"/>
                <DataGridTextColumn Width="*" Header="Description" Binding="{Binding Path=CategoriesDescription}"/>
                <DataGridTextColumn Width="*" Header="LastChangedDateTime" Binding="{Binding Path=LastChangedDateTime}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
